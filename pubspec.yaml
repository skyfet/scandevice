name: scandevice
description: Flutter plugin for ScanDevice SDK.
version: 1.0.0
repository: https://github.com/skyfet/scandevice

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: ru.siliconswords.scandevice
        pluginClass: ScandevicePlugin